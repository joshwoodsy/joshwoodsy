{
  "name": "pickup-driver-pwa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Phase 1 inbound pickup driver PWA (read-only, trip-number-first)",
  "scripts": {
    "start": "node server.js",
    "generate-icons": "node scripts/generate-icons.mjs",
    "test": "node --test test/api.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
